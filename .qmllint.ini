[General]
DisableDefaultImports=false
MaxWarnings=-1

[Warnings]
# Quickshell false positives qmlls cannot fix (PanelWindow backend, Widgets internals).
UncreatableType=disable
ImportFailure=disable
# PanelWindow grouped props (margins) and Hyprland re-exports (GlobalShortcut) stay unresolved.
UnqualifiedAccess=disable
UnresolvedType=disable
